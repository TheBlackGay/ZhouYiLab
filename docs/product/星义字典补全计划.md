# 星义字典补全计划（D4=A，2026-09-20 立项）

> 决策：用户拍板 D4=**A 逐步补全**（研究性任务）。内核符号全集 109 星，
> `config/ziwei/symbolism_dictionary.json` 现收 67，**缺 42**（全部为神煞系，
> 均不在 star_temperament 34 星内）。claim 与 `dictionary_version` 不动，
> 每批入库再升版。

> **进度 2026-09-21**：批次 1（长生十二神 12 条）已入库，67→79，`dictionary_version`
> 1.0.0→**1.1.0**。三重交叉锁见 `test_ziwei_symbols_contract.test_changsheng12_batch1_cross_locks`
> （次序锁内核 `ChangSheng12` 枚举；id 锁 `shen_sha_dictionary.chang_sheng_12`；禁吉凶断语词）。
> 释义取十二长生通行循环通义（不编《三命通会》卷次），source_ref 精确卷次**待联网校勘后补**
> ——本次 web 工具连续故障，出处定位以 `attributes.note` 诚实挂"待校勘"，宁缺毋伪。
> 
> **进度 2026-09-21（同日二）**：批次 2（博士/岁前/将前三系年煞 **28 条**）入库，79→**107**，
> 版本 **1.2.0**。三系 **36 个 memberships**（含大耗/龙德/天德/华盖/咸池 5 个早收旧条目补挂）
> 逐位 `attributes.orders` 锁内核枚举；新条目 id 锁职能层（重名重 id 者文档化后缀消歧：
> 岁前**官符**`guan_fu_sui_qian`、将前**息神**`xi_shen_jiang_qian`——注意与博士系"官府/喜神"
> 是异名同音不同神）；小耗、病符跨博士/岁前双系，单条目双 orders。derived 的 meaning/boundary
> 直接沿用职能层审定文本。锁：`test_shensha_batch2_cross_locks`。
> **批次 3 收尾仅余 2 星：天解、截空**（安法出处待考，宁缺毋伪暂不入库）+ 全典《三命通会》
> 卷次 source_ref 联网补录。

## 分批（按校勘成本从低到高）

### 批次 1 · 长生十二神（12 星，释义有定本）

长生、沐浴、冠带、临官、帝旺、衰、病、死、墓、绝、胎、养

释义来源：十二长生本身即"长生→养"的生旺墓绝循环语义，与内核十二长生
（`twelve_stages`，bazi-rules/0.1 已核字段）同构可交叉锁定——每条目
`attributes` 直接引内核阶段序，`original_definitions` 按《三命通会》卷上
通例释义撰写并挂 source_ref（D8 三层）。

### 批次 2 · 岁前/将前系年煞（约 20 星，安法已在引擎）

白虎、青龙、丧门、吊客、官符、太岁（岁建）、博士、力士、青龙/小耗、大耗、
指背、天煞、灾煞、劫煞、亡神、月煞、咸池…（以 `all_star_names` 与安星表实际差集为准）

安星位置可由内核神煞模块反查（`ba_zi_shen_sha`/紫微神煞安星法），释义逐条
校《三命通会》《紫微斗数全书》通行本；**吉凶色彩词一律按人性化画像纪律禁入**
（只述职司与所主之象，不写"主凶丧"式断语——与平台"断语押后"总纲一致）。

### 批次 3 · 杂系（截空、天解、解神、旬中空亡类、飞廉、贯索、息神、攀鞍、奏书、喜神等）

安法散出，逐星先补"安星出处"再补象义；无可靠出处者按 D4 候选 B 处理为
"标注未收录"，**宁缺毋伪**。

## 每条目的最低标准（沿用现有 schema）

```
id / name / category=sha / system（岁前|将前|长生|杂）/ attributes{yin_yang?, element?}
original_definitions[]: {concept, definition}  ≥1 条
source_ref: {book, chapter}  ≥1 条（D8 三层，禁止编造）
entry_revision 从 1 起；校勘后递增
```

## 完成定义

批次 2/3 入库后跑 `test_ziwei_symbols_contract.py`（收录覆盖断言将升级为
`missing == []` 或显式豁免清单）；`dictionary_version` 1.0.0 → 1.1.0。
