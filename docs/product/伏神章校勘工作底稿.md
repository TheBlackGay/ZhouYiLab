# 伏神章校勘工作底稿（DEF-5 主线，待原文开跑）

> 立项背景：D1=A（2026-09-20）拍板以《增删卜易》为主口径后，进退神四对、
> 乾坤纳干已收口，**唯余伏神"出不出、破空可否用"**（台账 §3 DEF-5）。
> 本底稿固定"原文一到即开工"的全部结构与验收，原文栏留白——
> **纪律：不凭记忆成稿**（web_search/web_fetch 连续多轮故障，任何"背诵原文"
> 都属发明口径，平台禁止）。

## 一、输入（待供给，二选一即可开跑）

- [ ] 用户贴《增删卜易》伏神章逐字原文（推荐：纸质书页拍照转文亦可）；
- [ ] 或网络工具恢复后由代理检索比对（候选库：维基文库/国学会/可借古籍影像，
      须注明**版本**，异文逐条登记）。

需要覆盖的论题清单（引擎现状反推，缺哪条补哪条）：

| # | 论题 | 引擎现状 | 校勘后要回答 |
| --- | --- | --- | --- |
| T1 | 伏神取法（何六亲伏何爻下） | ✅ 已实现：本宫纯卦六亲不现者，伏于本卦同序号爻下（rule_profile `fu_shen` 已定述） | 与原文逐句核对，若有出入登记异文 |
| T2 | **"出不出"**（伏神何时可提引） | ❌ 未实现（DEF-5 核心） | 得日月生扶/冲飞/出空/入墓等何条件时"出"？ |
| T3 | **"破空可否用"** | ❌ 未实现 | 伏神值月破/旬空，究竟有用无用？原文判据 |
| T4 | 飞神关系（克生合墓） | 部分：`入飞神墓` 标签已有（飞静不空不破条件） | 飞来克伏/伏去生飞等判据是否入标 |
| T5 | 伏与用神/世应关系 | ❌ 未实现 | 用神伏 vs 非用神伏的处理差异 |

## 二、落点（校勘结论逐条映射，先契约后行为）

1. **条款台账**：本文件 §五 登记表（每条：原文引文 → 白话判据 → 代码落点 → 状态）；
2. **内核**：`src/liu_yao/liu_yao.cppm` 状态标签函数增设 `fu_out`/`fu_usable` 类
   标记（命名待定），仅在 T2/T3 有逐字依据后实现；`rule_profile` 升
   `liu-yao-rules/0.3`，`fu_shen`/`state_tags_scope` 条目改写为"出不出=校勘后口径"；
3. **回归锁**：`tests/test_liuyao_jin_tui_contract.py` 同风格新增扫描锁 +
   `tests/test_liuyao_hexagram_table_contract.py` 模式的金样例；每条校勘结论
   至少一正一反两例；
4. **案例库**：结论入 `config/liuyao/cases/`（structural_regression 锁行为）；
   古籍验例同步以 `classic_example + pending_manual_collation → self_consistent`
   流程升级（诚实闸已内置于 schema/测试）；
5. **画像端**：伏神标签若新增，`web/liuyao_distribution.py` 的 basis 文案与
   `config/liuyao/distribution_reading.json` 同步，且画像不新增断语（禁断语纪律）；
6. **文档四处联动**：README 六爻行、`docs/product/待确认决策与完善清单.md`
   §0/§3（DEF-5 摘牌）、`docs/版本管理规范.md` §6（若构成行为变更记入下个
   版本注记）、校准套件登记。

## 三、验收标准

- 每个实现的行为，其原文引文逐字可溯（版本+页/章），无"大意如此"；
- 异文（版本间文字差异）逐条登记并选主从、说明理由；
- 引擎只标事实状态的平台边界不变：**"可否用"若原文语义属占断规则，
  则实现为事实标签（如 `fu_de_yue_sheng`），断语仍由画像层禁出**；
- `python3 -m unittest discover -s tests` 全绿 + `check_calibration.py` 通过；
- 六爻 claim 是否升"已校准"仍是独立人工决策（另需古籍验例入库，A3 收尾）。

## 四、工序（原文到位当日）

```
① 全文逐句断号（编号 X1..Xn）→ 填入 §五 表"原文"列
② 每条判据白话化，与引擎现状 diff → 标记 [已有/新增/冲突/不入码(占断专属)]
③ "冲突"与"新增"逐条过 D1 式拍板（用户确认才动码）
④ 按 §二 落点实施 → 回归锁 → 四处文档联动 → 单独提交（不与无关变更混合）
⑤ DEF-5 状态改写；若伏神行为有变，同批评估 liuyao 案例库补例与 claim 材料
```

## 五、条款登记表（空表待原文）

| # | 原文引文（逐字+版本页码） | 白话判据 | 处置 | 代码落点 | 回归例 |
| --- | --- | --- | --- | --- | --- |
| X1 | （待输入） | | ⬜ | | |
| X2 | （待输入） | | ⬜ | | |

> 同场顺带（同为网络恢复后的校勘批次）：梅花书例文字核对（含
> 《梅花易数》文档中"翼氏大典"书目存疑项 TODO-引）、星义字典《三命通会》
> 卷次 source_ref 补录、六爻古籍验例采集。
