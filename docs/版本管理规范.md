# ZhouYiLab 版本管理规范

> 当前项目版本：`1.4.1`
> 生效日期：`2026-08-31`

## 1. 版本格式

项目版本统一使用：

```text
主版本.功能版本.修订版本
MAJOR.MINOR.PATCH
```

例如：`1.1.0`、`1.2.0`、`1.2.1`。

## 2. 升级规则

### 2.1 功能大改

新增完整功能、进行较大范围改造或形成新的业务能力时，递增功能版本，并把修订版本归零。

```text
1.1.0 → 1.2.0 → 1.3.0
```

适用示例：

- 新增完整命盘分析引擎；
- 将现有功能改造成前后端分离接口；
- 新增完整术数模块；
- 对核心计算或数据结构进行较大改造。

### 2.2 Bug 修复与小优化

修复缺陷、调整文案、优化局部性能或进行不改变主体能力的小范围改进时，递增修订版本。

```text
1.1.0 → 1.1.1 → 1.1.2
```

适用示例：

- 修复某个流月宫位计算错误；
- 修正接口字段或错误提示；
- 优化页面局部布局；
- 补充少量格局配置，但不改变分析架构。

### 2.3 主版本升级

发生整体架构替换、核心算法或公共契约大面积不兼容时，递增主版本，并将后两位归零。

```text
1.9.3 → 2.0.0
```

主版本升级必须在实施前确认迁移范围，不能仅因为功能数量增加而随意升级。

## 3. 分支与提交

版本分支统一使用：

```text
r_<版本号>
```

例如：

```text
r_1.1.0
r_1.1.1
r_1.2.0
```

提交信息应描述该版本的主要结果：

```text
feat: release ZhouYiLab 1.1.0 API foundation
fix: release ZhouYiLab 1.1.1 calculation corrections
```

## 4. 发布时必须同步

每次创建版本分支并提交前，必须同步检查：

1. 根目录 `CMakeLists.txt` 的 `project(... VERSION ...)`；
2. HTTP API 返回的 `algorithm_version`；
3. 相关接口或功能文档的版本号；
4. 示例程序与自动化测试；
5. Git 分支名是否与项目版本一致；
6. 工作区是否包含不属于本版本的文件。

## 5. 项目版本与 API 版本

项目版本和 HTTP API 路径版本不是同一个概念：

- 项目可以从 `1.1.0` 升级到 `1.2.0`，接口仍保持 `/api/v1`；
- 只要 `/api/v1` 继续保持向后兼容，就不需要升级 API 路径；
- 只有接口出现无法兼容的字段或语义变化时，才新增 `/api/v2`；
- 旧 API 应保留迁移期并明确标记废弃时间。

## 6. 当前版本记录

### 1.4.1

- 修复格局结果在结构解读页面入口过深、容易被误认为未展示的问题；
- 增加本命格局总览、逐宫格局计数和直接跳转入口。

### 1.4.0

- 新增配置驱动的紫微斗数格局匹配引擎；
- 格局按盘层和焦点宫独立匹配并输出条件证据；
- 星曜组合与格局规则拆分维护、拆分展示；
- 旧 C++ 格局结果降为接口兼容数据，不再作为结构化分析依据。

### 1.1.0

- 紫微斗数排盘能力改造成版本化 HTTP API；
- C++ 桥接改用 JSON 请求；
- 运限支持按层级请求；
- 建立统一成功、错误与元数据响应；
- 建立命盘与运限分析准则；
- 建立可扩展的静态分析案例配置；
- 输出紫微斗数 HTTP API Markdown 文档。
