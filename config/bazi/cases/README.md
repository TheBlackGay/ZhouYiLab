# 八字校准命例库（D3 主线：子平格局派）

流程口径（三步走，禁止本末倒置）：
1. 定性判断边界：月令→根气（强/中/微）→天干之势→刑冲合会，判"偏强/中和/偏弱"档位（不打分）；
2. 用神取用逻辑：扶抑 与 调候 的优先级判定；
3. 定性稳定后，才引入旺衰量化（日主强度指数）作为辅助输出维度。

规则：
- 每条命例一个 JSON 文件（schema：bazi-case/1.0），judgment 在第 1 步完成前允许留空；
- expected_facts 先行进 CI（排盘事实回归）；judgment 齐备且 verified 的条数达到门槛前，
  manifest 的 bazi claim 保持 in_progress（升级由人工决策，check_calibration 不推断）；
- 平台明确不做盲派"做功直断"的规则模拟（D3 边界）。
